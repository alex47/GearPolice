GearPolice TODO
===============

Medium priority
---------------

- Clear the tracked list when transitioning from solo to grouped.
  When GROUP_ROSTER_UPDATE shows the local player went from not grouped to
  party or raid, clear existing PlayerGearInfo and any queued or active scan
  work before tracking the new roster, so solo/target data cannot remain
  visible in the new group scan.

Cleanup
-------

- Avoid direct AceGUI widget internals where practical.
  UI.lua mutates widget internals such as playerNameLabel.label and itemIcon.frame
  backdrop state. AceGUI documentation warns that direct frame mutations can leak
  through pooled widgets.

- Decide whether Shared.lua should contain shared code or be removed from the
  load list.
