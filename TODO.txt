GearPolice TODO
===============

Low priority
------------

- Show problematic item details in the UI.
  Make it visible in the UI which equipped items are problematic and why, for
  example missing gem, missing enchant, low item level, or missing upgrade.

Cleanup
-------

- Refactor and reorganize addon modules.
  Core.lua still contains several responsibilities from the early project stages:
  roster reconciliation, scan queue dispatch, scan lifecycle, timer ownership,
  slash commands, and event handlers. Split these into focused modules
  incrementally, preserving behavior and verifying each extraction. See
  REFACTOR.md for the detailed roadmap.
